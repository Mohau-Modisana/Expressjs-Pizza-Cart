<div>

<h1 class="center">Perfect~Pizza</h1>
    <div class="center rad">
        <img class="rad" src="img/logo.gif" width="150" >
    </div>
<h2 class="center">Order details</h2>

<form action="/login" method="Post">
    <div class="center hide">
        <div>
            <span>
                Name:      
            </span>
            <span>
                <input type="text"  name="OrderName" value="{{login.name}}" placeholder="Who's ordering" class="">
            </span>
        </div>



        <div>
            <span>
                Contacts:      
            </span>
            <span>
                <input type="text"  name="Contacts" value="{{login.contacts}}" placeholder="What's your Phone number" class="">
            </span>
        </div>

        <div>
            <span>
                Address:  
            </span>
            <span>
                <input type="text"  name="Address" value="{{login.address}}" placeholder="What's your location" class="">
            </span>
        </div>

        <div>
            <button type="submit" name="button" class="theButton pay paybox">Let's Order</button>
        </div>
        </form>

    </div>

</div>